# Collections

Saved You-i Design DNA lives here as `{slug}.md` (for Cursor) and `{slug}.json` (raw).

After you save a design in the app, mention it in Cursor:

`@you-i dashboard-app use this design inspiration to design the website`

Moodboard photos in a collection are inspiration only. Do not drop them into the generated UI.
